<UserControl x:Class="FastBackup.Plans.PlanOverview"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FastBackup.Plans"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Button x:Name="Create" Content="Create plan" HorizontalAlignment="Left" Margin="639,30,0,0" VerticalAlignment="Top" Height="30" Width="122" Command="{Binding CreateCommand}"/>
        <ListView x:Name="PlansList" Margin="187,10,390,13" ItemsSource="{Binding Plans}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
